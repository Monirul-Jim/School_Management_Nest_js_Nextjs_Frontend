/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'randomuser.me',
      'images.unsplash.com',
      'plus.unsplash.com',
      'media.istockphoto.com'
    ],
  },
};

module.exports = nextConfig;
