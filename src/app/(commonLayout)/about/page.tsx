import About from "@/components/Home/About/About";

// Metadata for the About page
export const metadata = {
  title: "About SchoolMS | School Management System",
  description: "Learn more about SchoolMS, the modern school management system for students, teachers, and schools.",
};

const AboutPage = () => {
  return <About />;
};

export default AboutPage;
